# Hoist CDK deps so aws-cdk-lib's internal require() resolves (time/ uses npm's flat layout)
public-hoist-pattern[]=*aws-cdk*
public-hoist-pattern[]=*constructs*
public-hoist-pattern[]=fs-extra
public-hoist-pattern[]=minimatch
