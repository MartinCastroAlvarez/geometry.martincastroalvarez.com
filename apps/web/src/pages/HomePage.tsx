/**
 * Home page (landing).
 *
 * Context: Rendered at /. Currently a placeholder (returns null). Can be extended
 * for marketing or redirect.
 *
 * Example:
 *   <Route path="/" element={<HomePage />} />
 */
export const HomePage = () => {
    return null;
};
